{
  "name": "gl-changesets",
  "version": "0.0.1",
  "description": "Visualize OSM changesets on a GL map (WIP)",
  "main": "js/index.js",
  "scripts": {
    "start": "npm run serve & npm run watch",
    "watch": "watchify --debug js/index.js -o dist/bundle.js",
    "build": "browserify js/index.js -o dist/bundle.js",
    "serve": "http-server .",
    "lint": "eslint js/*.js"
  },
  "author": "Mapbox",
  "license": "ISC",
  "dependencies": {
    "mapbox-gl": "^0.18.0",
    "moment": "^2.11.2",
    "turf-buffer": "^3.0.5",
    "xhr": "^2.2.0"
  },
  "devDependencies": {
    "browserify": "^13.0.0",
    "eslint": "^2.12.0",
    "eslint-config-mourner": "^2.0.1",
    "http-server": "^0.9.0",
    "watchify": "^3.7.0"
  }
}
