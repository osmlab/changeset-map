{
  "name": "gl-changesets",
  "version": "0.0.1",
  "description": "Visualize OSM changesets on a GL map (WIP)",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "watchify --debug js/index.js -o dist/bundle.js",
    "build": "browserify js/index.js -o dist/bundle.js"
  },
  "author": "Mapbox",
  "license": "ISC",
  "dependencies": {
    "mapbox-gl": "^0.14.2",
    "moment": "^2.11.2",
    "watchify": "^3.7.0",
    "xhr": "^2.2.0"
  }
}
