{
  "name": "changeset-map",
  "version": "0.0.1",
  "description": "Visualize OSM changesets on a GL map (WIP)",
  "private": true,
  "main": "js/index.js",
  "scripts": {
    "start": "npm run serve & npm run watch",
    "watch": "watchify --debug js/render.js -o dist/bundle.js",
    "build": "browserify js/render.js -o dist/bundle.js",
    "serve": "http-server ."
  },
  "author": "Mapbox",
  "license": "ISC",
  "dependencies": {
    "mapbox-gl": "^0.28.0",
    "moment": "^2.17.1",
    "turf-buffer": "^3.0.12",
    "xhr": "^2.3.1"
  },
  "devDependencies": {
    "browserify": "^13.1.1",
    "http-server": "^0.9.0",
    "watchify": "^3.7.0"
  }
}
